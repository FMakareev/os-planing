mutation($content: String!, $title: String!, $index: Int!){
    createPrivacy(content: $content, title: $title, index: $index){
        privacy{
            id
            title
            content
            index
        }
    }
}