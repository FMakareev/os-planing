query($page: Int!,$limit: Int!){
    receptionPagination(page: $page, limit: $limit) {
        pageInfo {
            nextPage
            prevPage
        }
        count
        items {
            user {
                id
            }
        }
    }
}