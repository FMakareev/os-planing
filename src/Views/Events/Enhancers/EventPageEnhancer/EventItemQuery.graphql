query ($id: String!) {
    eventItem(id: $id) {
        date
        id
        projects {
            id
            name
        }
        reception {
            city
            id
        }
        report
        status
        statusUpdated
        text
        title
        attachments {
            id
            url
            name
            ext
            size
        }
    }
}
